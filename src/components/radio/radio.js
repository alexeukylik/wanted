import React from 'react';
import Input from '../input/input';

const Radio = (props) => {
    return (
        <React.Fragment>
            <Input {...props}/>
        </React.Fragment>
    );
};

export default Radio;